## MarketDataService scanner tuning (no secrets)
##
## Copy to `MarketDataService/.env` and tweak values.
## Start with: `uvicorn app:app --reload --port 8001 --env-file .env`

# Redis cache (optional)
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=300

# Scanner baseline
MIN_PRICE_FLOOR=1.5

## Relative volume method: day-trader friendly 1-minute rolling RVOL
##
## Definition:
##   RVOL = sum(last K 1m volumes) / (K * avg_1m_volume_baseline)
##
## Baseline avg_1m_volume is computed from:
##   - prior N full regular sessions (default 1)
##   - optionally today's regular-session bars too (excluding the last K bars by default)
##
## Only `REL_VOL_HISTORY_DAYS` of 1m data is downloaded (default 2 days).
REL_VOL_METHOD=recent_k_1m
REL_VOL_INTERVAL=1m
REL_VOL_HISTORY_DAYS=2
REL_VOL_BASELINE_DAYS=1
REL_VOL_K_BARS=5
REL_VOL_BASELINE_INCLUDE_TODAY=1
REL_VOL_BASELINE_EXCLUDE_LAST_K=1

## If your scanners are already using interval=1m, this avoids a second 1m download by
## fetching `REL_VOL_HISTORY_DAYS` in the primary intraday request.
REL_VOL_REUSE_PRIMARY_1M_DOWNLOAD=1
